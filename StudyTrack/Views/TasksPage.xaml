<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:StudyTrack.ViewModels"
             xmlns:models="clr-namespace:StudyTrack.Models"
             x:Class="StudyTrack.Views.TasksPage"
             x:DataType="vm:TasksViewModel"
             Title="My Tasks"
             BackgroundColor="#F9FAFB">

    <Grid RowSpacing="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Filter Section -->
        <VerticalStackLayout Grid.Row="0" Padding="20" Spacing="10" BackgroundColor="White">
            <Entry Placeholder="Search tasks..." Text="{Binding SearchQuery}" />
            <Picker ItemsSource="{Binding FilterOptions}" SelectedItem="{Binding SelectedFilter}" Title="Filter by" />
        </VerticalStackLayout>

        <!-- Tasks List -->
        <CollectionView Grid.Row="1" ItemsSource="{Binding FilteredTasks}" Margin="20,10">
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="models:TodoTask">
                    <Border BackgroundColor="White" Padding="15" Margin="0,0,0,10">
                        <Border.StrokeShape>
                            <RoundRectangle CornerRadius="12" />
                        </Border.StrokeShape>
                        <Border.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type vm:TasksViewModel}}, Path=GoToTaskDetailCommand}"
                                                  CommandParameter="{Binding .}" />
                        </Border.GestureRecognizers>
                        <Grid ColumnSpacing="10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <CheckBox Grid.Column="0" IsChecked="{Binding IsCompleted, Mode=OneWay}" />
                            <VerticalStackLayout Grid.Column="1" Spacing="5">
                                <Label Text="{Binding Title}" FontSize="16" FontAttributes="Bold" />
                                <Label Text="{Binding SubjectName}" FontSize="14" TextColor="#6B7280" />
                                <Label Text="{Binding DueDateFormatted}" FontSize="12" TextColor="{Binding PriorityColor}" />
                            </VerticalStackLayout>
                        </Grid>
                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <Button Grid.Row="1" Text="+ Add Task"
                Command="{Binding GoToAddTaskCommand}"
                BackgroundColor="#3B82F6"
                TextColor="White"
                Padding="15"
                Margin="20"
                VerticalOptions="End" />
    </Grid>

</ContentPage>
